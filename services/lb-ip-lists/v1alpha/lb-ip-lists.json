{
  "components": {
    "schemas": {
      "DeleteIPListResponse": {
        "description": "DeleteIPListResponse returns an empty body if successful.",
        "type": "object"
      },
      "GetIPListResponse": {
        "description": "GetIPListResponse returns IP list configuration without CIDR entries.",
        "properties": {
          "contentHash": {
            "description": "Unique SHA-256 hex fingerprint used to verify the stored IP list content.",
            "example": "3e0a9ef962b07c2e24d321c075ebb88c261bae231f9ba34d6ffb003619f9645c",
            "format": "string",
            "pattern": "^[a-f0-9]{64}$",
            "type": "string"
          },
          "labels": {
            "additionalProperties": {
              "type": "string"
            },
            "description": "Labels represent user-defined metadata as key-value pairs.\nLabel count should not exceed 64 per IP list.\n\n**Key Formatting Rules:**\nLength: 1-63 characters.\nCharacters: Must begin and end with [a-zA-Z0-9]. May contain dashes (-), underscores (_), dots (.), and alphanumerics in between.\nKeys starting with 'stackit-' are system-reserved; users MUST NOT manage them.\n\n**Value Formatting Rules:**\nLength: 0-63 characters (empty string explicitly allowed).\nCharacters (for non-empty values): Must begin and end with [a-zA-Z0-9]. May contain dashes (-), underscores (_), dots (.), and alphanumerics in between.\n",
            "example": {
              "cost.center": "billing.dept",
              "environment": "prod",
              "name": "my-app",
              "region": ""
            },
            "type": "object"
          },
          "name": {
            "description": "IP list name.",
            "example": "my-ip-list",
            "pattern": "^[0-9a-z](?:(?:[0-9a-z]|-){0,49}[0-9a-z])?$",
            "type": "string"
          },
          "numberOfIps": {
            "description": "Number of IPv4 CIDR entries stored in the list.",
            "example": 1234,
            "maximum": 50000,
            "minimum": 1,
            "type": "string"
          },
          "usage": {
            "allOf": [
              {
                "$ref": "#/components/schemas/IPListUsage"
              }
            ],
            "description": "Current use of this IP list in load balancers."
          }
        },
        "required": [
          "name"
        ],
        "type": "object"
      },
      "GetQuotaResponse": {
        "description": "GetQuotaResponse returns the configured IP list quota for the project and region.",
        "properties": {
          "projectId": {
            "description": "Project identifier",
            "example": "2438ac3c-37eb-4902-adef-ed16b4431030",
            "format": "uuid",
            "pattern": "^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$",
            "readOnly": true,
            "type": "string"
          },
          "quotas": {
            "$ref": "#/components/schemas/Quotas"
          },
          "region": {
            "description": "Region",
            "example": "eu01",
            "format": "string",
            "pattern": "^[a-z]{2,4}[0-9]{2}$",
            "readOnly": true,
            "type": "string"
          }
        },
        "type": "object"
      },
      "GoogleProtobufAny": {
        "additionalProperties": true,
        "description": "Contains an arbitrary serialized message along with a @type that describes the type of the serialized message.",
        "properties": {
          "@type": {
            "description": "The type of the serialized message.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "IPListUsage": {
        "description": "IPListUsage describes which NLB/ALB currently reference an IP list.",
        "properties": {
          "count": {
            "description": "Number of load balancers currently using this IP list.",
            "format": "int32",
            "type": "integer"
          },
          "items": {
            "description": "List of load balancers that reference this IP list.",
            "items": {
              "$ref": "#/components/schemas/UsageItem"
            },
            "type": "array"
          }
        },
        "type": "object"
      },
      "IPListsQuota": {
        "description": "IPListsQuota holds the limits and current usage for IP list resources.\n Defaults: limit=3 per project, max_entries_per_list=50000.",
        "properties": {
          "limit": {
            "description": "The maximum number of IP lists allowed in this project. Defaults to 3. Use -1 to indicate unlimited.",
            "format": "int32",
            "maximum": 3,
            "minimum": -1,
            "title": "Max IP lists",
            "type": "integer"
          },
          "maxEntriesPerList": {
            "description": "The maximum number of IPv4 CIDR entries allowed in a single IP list. Defaults to 50000.",
            "format": "int32",
            "maximum": 50000,
            "minimum": -1,
            "title": "Max entries per IP list",
            "type": "integer"
          },
          "usage": {
            "description": "The number of IP lists that currently exist in this project.",
            "format": "int32",
            "maximum": 1000000,
            "title": "Used IP lists",
            "type": "integer"
          }
        },
        "type": "object"
      },
      "ListIPListsResponse": {
        "description": "ListIPListsResponse returns metadata for all IP lists in a project and region.\n The CIDR entries themselves are never returned.",
        "properties": {
          "items": {
            "description": "Every IP list in the project and region. Never includes the CIDR entries themselves.",
            "items": {
              "$ref": "#/components/schemas/GetIPListResponse"
            },
            "type": "array"
          }
        },
        "type": "object"
      },
      "Quotas": {
        "description": "Quotas holds the specific quota elements for resources managed by this API.",
        "properties": {
          "ipLists": {
            "$ref": "#/components/schemas/IPListsQuota"
          }
        },
        "type": "object"
      },
      "Status": {
        "description": "The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors).",
        "properties": {
          "code": {
            "description": "The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].",
            "format": "int32",
            "type": "integer"
          },
          "details": {
            "description": "A list of messages that carry the error details.  There is a common set of message types for APIs to use.",
            "items": {
              "$ref": "#/components/schemas/GoogleProtobufAny"
            },
            "type": "array"
          },
          "message": {
            "description": "A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "UploadIPListPayload": {
        "description": "Request payload for creating or replacing an IP list.\n\n**Limits \u0026 Restrictions:**\n- **Project Quota:** Maximum of 3 IP lists allowed per project.\n- **Payload Size:** The entire HTTP request body JSON payload is restricted to a maximum size of 2 MB. This limit applies to the full JSON structure, including all fields (`name`, `projectId`, `region`, `fileContent`) and all `labels` keys and values.\n- **Rate Limit:** 1 request per minute.",
        "properties": {
          "fileContent": {
            "description": "Raw upload payload. Base64-encoded over JSON. Newline-separated `.txt` list of IPv4 CIDR entries, or CSV with `cidr,labels` columns (any other columns are ignored).",
            "format": "byte",
            "type": "string"
          },
          "labels": {
            "additionalProperties": {
              "type": "string"
            },
            "description": "Labels represent user-defined metadata as key-value pairs.\nLabel count should not exceed 64 per IP list.\n\n**Key Formatting Rules:**\nLength: 1-63 characters.\nCharacters: Must begin and end with [a-zA-Z0-9]. May contain dashes (-), underscores (_), dots (.), and alphanumerics in between.\nKeys starting with 'stackit-' are system-reserved; users MUST NOT manage them.\n\n**Value Formatting Rules:**\nLength: 0-63 characters (empty string explicitly allowed).\nCharacters (for non-empty values): Must begin and end with [a-zA-Z0-9]. May contain dashes (-), underscores (_), dots (.), and alphanumerics in between.\n",
            "example": {
              "cost.center": "billing.dept",
              "environment": "prod",
              "name": "my-app",
              "region": ""
            },
            "type": "object"
          },
          "name": {
            "description": "IP list name",
            "example": "my-ip-list",
            "maxLength": 51,
            "pattern": "^[0-9a-z](?:(?:[0-9a-z]|-){0,49}[0-9a-z])?$",
            "type": "string"
          },
          "projectId": {
            "description": "Project identifier",
            "example": "2438ac3c-37eb-4902-adef-ed16b4431030",
            "format": "uuid",
            "pattern": "^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$",
            "readOnly": true,
            "type": "string"
          },
          "region": {
            "description": "Region",
            "example": "eu01",
            "format": "string",
            "pattern": "^[a-z]{2,4}[0-9]{2}$",
            "readOnly": true,
            "type": "string"
          }
        },
        "required": [
          "name",
          "fileContent"
        ],
        "type": "object"
      },
      "UsageItem": {
        "description": "IPListUsageItem names the LB referencing the IP list.",
        "properties": {
          "loadBalancerName": {
            "description": "The display name of the load balancer.",
            "example": "my-alb",
            "type": "string"
          }
        },
        "required": [
          "loadBalancerName"
        ],
        "type": "object"
      }
    }
  },
  "info": {
    "description": "This API manages IP lists (sets of IPv4 CIDR ranges) scoped to a project and region. IP lists enable blacklisting: they can be referenced by Application and Network Load Balancer to deny traffic from matching source addresses.\n\n**Upload format**\nLists are uploaded as a single raw payload in `file_content` (base64 in JSON transport). The server first tries to parse the payload as CSV (with a `cidr` column; any other columns are ignored) and falls back to a newline-separated `.txt` list of CIDR entries when CSV parsing fails.\n\n**Limits**\n- Maximum request payload size: 2 MB JSON (enforced before parsing).\n- Maximum number of CIDR entries per list: 50000 (enforced during streaming parse).\n- Maximum number of IP lists per project: 3.\n- Empty lists (0 CIDRs) are rejected.\n- Only IPv4 CIDR entries are supported for now; IPv6 entries are rejected.\n- No duplicate or overlap checks are performed.\n\n**Semantics**\n- `Upload` creates the list if it does not exist and replaces its content otherwise.   Uploads with identical stored content are a no-op (no version bump, no reload).\n- `List` returns each stored IP list without its CIDR entries (name, labels, number of IPs, content hash, usage).   The CIDR entries themselves are never returned by the API.\n- There is no single-item Get endpoint. Clients verify content by comparing the returned `content_hash`.\n- IP lists referenced by any load balancer cannot be deleted; the reference must be cleared or   replaced with a different list first.\n",
    "title": "STACKIT Load Balancer IP Lists API",
    "version": "1alpha.0.0"
  },
  "openapi": "3.0.3",
  "paths": {
    "/v1alpha/projects/{projectId}/regions/{region}/ip-lists": {
      "get": {
        "description": "ListIPLists returns every IP list in the project without CIDR entries.\n No pagination while the per-project limit is 3.",
        "operationId": "ListIPLists",
        "parameters": [
          {
            "in": "path",
            "name": "projectId",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "region",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ListIPListsResponse"
                }
              }
            },
            "description": "OK"
          },
          "400": {
            "description": "One or more fields are invalid."
          },
          "401": {
            "content": {
              "*/*": {
                "schema": {
                  "$ref": "#/components/schemas/Status"
                }
              }
            },
            "description": "Unauthorized"
          },
          "403": {
            "description": "this project_id is not allowed to try this API"
          },
          "500": {
            "description": "internal error - please retry again later or contact support if the issue persists"
          },
          "default": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Status"
                }
              }
            },
            "description": "Default error response"
          }
        },
        "summary": "List metadata of all IP lists in a project",
        "x-stackit-authorization": {
          "actions": [
            "lb-ip-lists.loadbalancer.list"
          ],
          "resource-id": "projectId",
          "resource-id-type": "dynamic",
          "resource-type": "project",
          "service-enablement": {
            "services": [
              "cloud.stackit.loadbalancer"
            ]
          }
        }
      }
    },
    "/v1alpha/projects/{projectId}/regions/{region}/ip-lists/{name}": {
      "delete": {
        "description": "Removes the IP list configuration from the project. If the IP list is referenced by load balancers,\n clear or replace the reference first.",
        "operationId": "DeleteIPList",
        "parameters": [
          {
            "in": "path",
            "name": "projectId",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "region",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "name",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DeleteIPListResponse"
                }
              }
            },
            "description": "OK"
          },
          "400": {
            "description": "IP list is referenced by a load balancer"
          },
          "401": {
            "content": {
              "*/*": {
                "schema": {
                  "$ref": "#/components/schemas/Status"
                }
              }
            },
            "description": "Unauthorized"
          },
          "403": {
            "description": "this project_id is not allowed to try this API"
          },
          "404": {
            "description": "IP list not found"
          },
          "500": {
            "description": "internal error - please retry again later or contact support if the issue persists"
          },
          "default": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Status"
                }
              }
            },
            "description": "Default error response"
          }
        },
        "summary": "Delete an IP List configuration",
        "x-stackit-authorization": {
          "actions": [
            "lb-ip-lists.loadbalancer.delete"
          ],
          "resource-id": "projectId",
          "resource-id-type": "dynamic",
          "resource-type": "project",
          "service-enablement": {
            "services": [
              "cloud.stackit.loadbalancer"
            ]
          }
        }
      },
      "get": {
        "description": "GetIPList returns metadata of a single IP list by name, without CIDR entries.",
        "operationId": "GetIPList",
        "parameters": [
          {
            "in": "path",
            "name": "projectId",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "region",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "name",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/GetIPListResponse"
                }
              }
            },
            "description": "OK"
          },
          "400": {
            "description": "One or more fields are invalid."
          },
          "401": {
            "content": {
              "*/*": {
                "schema": {
                  "$ref": "#/components/schemas/Status"
                }
              }
            },
            "description": "Unauthorized"
          },
          "403": {
            "description": "this project_id is not allowed to try this API"
          },
          "404": {
            "description": "ip list not found"
          },
          "500": {
            "description": "internal error - please retry again later or contact support if the issue persists"
          },
          "default": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Status"
                }
              }
            },
            "description": "Default error response"
          }
        },
        "summary": "Retrieve metadata of an existing IP list configuration in a project.",
        "x-stackit-authorization": {
          "actions": [
            "lb-ip-lists.loadbalancer.get"
          ],
          "resource-id": "projectId",
          "resource-id-type": "dynamic",
          "resource-type": "project",
          "service-enablement": {
            "services": [
              "cloud.stackit.loadbalancer"
            ]
          }
        }
      },
      "put": {
        "description": "UploadIPList creates the IP list or replaces its content. Raw upload\n is in `file_content`.",
        "operationId": "UploadIPList",
        "parameters": [
          {
            "in": "path",
            "name": "projectId",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "region",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "name",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UploadIPListPayload"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/GetIPListResponse"
                }
              }
            },
            "description": "OK"
          },
          "400": {
            "description": "One or more fields are invalid."
          },
          "401": {
            "content": {
              "*/*": {
                "schema": {
                  "$ref": "#/components/schemas/Status"
                }
              }
            },
            "description": "Unauthorized"
          },
          "403": {
            "description": "this project_id is not allowed to try this API"
          },
          "413": {
            "description": "attached content is too large"
          },
          "422": {
            "description": "unprocessable entity - parsed CIDR count exceeds the entry limit"
          },
          "429": {
            "description": "rate limit (1/min) exceeded, or the per-project maximum of 3 IP lists is reached"
          },
          "500": {
            "description": "internal error - please retry again later or contact support if the issue persists"
          },
          "default": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Status"
                }
              }
            },
            "description": "Default error response"
          }
        },
        "summary": "Create/Replace an IP list",
        "x-stackit-authorization": {
          "actions": [
            "lb-ip-lists.loadbalancer.upload"
          ],
          "resource-id": "projectId",
          "resource-id-type": "dynamic",
          "resource-type": "project",
          "service-enablement": {
            "services": [
              "cloud.stackit.loadbalancer"
            ]
          }
        }
      }
    },
    "/v1alpha/projects/{projectId}/regions/{region}/quotas": {
      "get": {
        "description": "Retrieves the IP List resource quota for the project.",
        "operationId": "GetQuota",
        "parameters": [
          {
            "in": "path",
            "name": "projectId",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "region",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/GetQuotaResponse"
                }
              }
            },
            "description": "OK"
          },
          "400": {
            "description": "One or more fields are invalid."
          },
          "401": {
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            },
            "description": "Unauthorized"
          },
          "403": {
            "description": "this projectId is not allowed to try this API"
          },
          "500": {
            "description": "internal error - please retry again later or contact support if the issue persists"
          },
          "default": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Status"
                }
              }
            },
            "description": "Default error response"
          }
        },
        "summary": "Get the quota for IP lists in a project",
        "x-stackit-authorization": {
          "actions": [
            "lb-ip-lists.quota.get"
          ],
          "resource-id": "projectId",
          "resource-id-type": "dynamic",
          "resource-type": "project",
          "service-enablement": {
            "services": [
              "cloud.stackit.loadbalancer"
            ]
          }
        }
      }
    }
  },
  "security": [
    {
      "bearerAuth": []
    }
  ],
  "servers": [
    {
      "url": "https://lb-ip-lists.api.{region}stackit.cloud",
      "variables": {
        "region": {
          "default": "eu01.",
          "enum": [
            "eu01.",
            "eu02."
          ]
        }
      }
    }
  ]
}